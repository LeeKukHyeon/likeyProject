server:
  port: '80'
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  
spring:
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://127.0.0.1:3306/ks44team03?serverTimezone=UTC&characterEncoding=UTF8
    username: cos
    password: cos1234
  thymeleaf:
    prefix: classpath:templates/
    suffix: .html
    cache: 'false'

mybatis:
  # classpath: => src/main/resources/
  # mapper.xml 파일 경로 
  mapper-locations: classpath:mapper/**/*.xml
  # mapper dto 설정
  type-aliases-package: ks44team03.dto
  
logging:
  config: classpath:logback-spring.xml
